package org.example.mapper;
import org.apache.ibatis.annotations.Param;
import org.example.entity.Room;

public interface RoomMapper {
    // 增加一个放映室
    //void AddRoom(Room);
    // 删除一个放映室
    //void DelRoom(Room room);
    // 查询是否存在当前编号的放映室
    //Room CheckRoom(int num);
    // 增加一个排片
    // 查询电影时间是否有效
}
